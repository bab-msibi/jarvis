import { Model } from "@/types/model";

// Intentionally no dummy/demo models here.
// The /models page loads real local/provider data via lib/server/live-models.ts.
export const models: Model[] = [];
